# BlueprintBridge
A local Windows IPC bridge that lets an external coding agent inspect and edit Unreal Editor assets, especially Blueprints, without driving the editor UI with mouse/screen automation.
